<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>Blog</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,line-clamp"></script>
</head>
<body>
    <div class="container border m-auto p-5">
        <h1 class="my-8 text-center text-4xl font-semibold">Blog Articles</h1>
        <nav class="flex justify-center items-center mb-6">
            <a href="/articles/new" class="bg-green-700 text-white py-2 px-4 m-3 rounded-sm">New Article</a>
            <form action="" class="border-2 border-gray-400 px-2 rounded-md">
                <label for="search" class="">
                    <input type="text" placeholder="Buscar" class="border-none p-1.5 outline-none">
                    <i class="fa-solid fa-magnifying-glass text-gray-500"></i>                </label>
            </form>
        </nav>

        <div class="grid grid-cols-4 gap-2">
            <% articles.forEach(article => { %>
                    <div class="border border-gray-500 p-3 text-center rounded-sm">
                        <h2 class="text-xl font-semibold mb-2"><%= article.title %></h2>
                        <h4 class="text-sm leading-normal mb-3 text-gray-600">
                        <small class="text-sm">Publicado el</small>    <%= article.createdAt.toLocaleDateString() %>
                        </h4>
                        <p class="mb-4"><%= article.description %></p>
                        <div class="flex justify-center gap-2">
                            <a href="articles/<%= article.slug %>" class="inline-block border bg-blue-800 text-white px-3 py-1 rounded-md mb-3">Read more</a>
                            <form action="/articles/<%= article.id %>?_method=DELETE" method="post" class="">
                                <button type="submit" class="inline-block bg-red-800 text-white px-3 py-1 rounded-md">Delete</button>
                            </form>
                        </div>
                    </div>
            <% }) %>
        </div>
    </div>
</body>
</html>